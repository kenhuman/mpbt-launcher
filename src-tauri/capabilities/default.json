{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default MPBT Launcher capabilities",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:path:default",
    "updater:default",
    "process:allow-restart",
    "shell:allow-open"
  ]
}
